// Planned Version

// General Purpose/Object Oriented
version "general"
version "object"

use "basic"

function main() {
    var name = (string) take("What's your name? ")
    sayLn("Hello, " + name + ".") // say("Hello, "); say(name); sayLn(".")
}
